# Blackjack with AngularJS

To run:

```
npm install
npm run
```

Then go to `http://localhost:8000/app/`.

To run the unit tests:

```
npm test
```

To run the e2e tests:

```
npm run protractor
```
